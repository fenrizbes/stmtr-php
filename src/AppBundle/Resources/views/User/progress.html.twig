{% if status == 'games' %}
    Updating the game list
    <img src="{{ asset('bundles/app/images/ball.gif') }}">
{% elseif status == 'achievements' %}
    Updating achievements: {{ percentage | number_format(1) }}%
    <img src="{{ asset('bundles/app/images/ball.gif') }}">
{% else %}
    {{ include('AppBundle:User:rating.html.twig', {
        user:  app.user,
        dates: true
    }) }}
{% endif %}