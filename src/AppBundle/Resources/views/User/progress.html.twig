{% if status in ['games', 'achievements'] %}
    <h2>your rating is being calculated.</h2>

    just give as a few seconds.
    <br /><br />

    {% if status == 'games' %}
        updating the game list
    {% else %}
        updating achievements: {{ percentage | number_format(1) }}%
    {% endif %}

    <img src="{{ asset('bundles/app/images/ball.gif') }}" />

{% else %}
    {{ include('AppBundle:User:result.html.twig', {
        user: app.user
    }) }}
{% endif %}